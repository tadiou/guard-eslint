require 'spec_helper'

describe Guard::Eslint do
  it 'has a version number' do
    expect(Guard::Eslint::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
